import 'report_attachment_picker_types.dart';

Future<SelectedReportAttachment?> pickReportAttachment() async {
  throw UnsupportedError(
    'La seleccion de comprobantes esta disponible en la version web.',
  );
}
